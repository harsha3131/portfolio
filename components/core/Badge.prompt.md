**Badge** — compact status or count pill.

```jsx
<Badge tone="solid">Published</Badge>
<Badge tone="success" dot>Live</Badge>
<Badge tone="neutral" size="sm">Draft</Badge>
```

Tones: `accent | neutral | success | warning | danger | solid` (solid = sunset gradient). `dot` adds a leading status dot.
